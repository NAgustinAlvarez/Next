export default function NotFound({ param }) {
  return (
    <main className="not-found">
      <h1>Meal not Found</h1>
      <p>Unfortunately we could not found the request page or meal data.</p>
    </main>
  );
}
